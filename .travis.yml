addons:
  postgresql: "9.3"

language: go

before_script:
  - createuser -U postgres --createdb --superuser ohtu
  - psql -c 'create database multi-axis-test;' -U postgres
  - psql multi-axis-test -c 'create schema public;' -U postgres
  - psql multi-axis-test -U postgres < test.dump.sql

